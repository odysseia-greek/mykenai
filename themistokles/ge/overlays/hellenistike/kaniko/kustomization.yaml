apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tekton-pipelines
resources:
  - ../../../base/kaniko

patches:
  - target:
      version: v1
      kind: Secret
      name: registry-creds
    path: secret.yaml
