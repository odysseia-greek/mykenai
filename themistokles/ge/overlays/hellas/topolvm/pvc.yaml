apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: topolvm-test-pvc
  namespace: test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: pyxis-delete
  resources:
    requests:
      storage: 1Gi