environments:
  local:
    values:
      - values/local.yaml
  staging:
    values:
      - values/staging.yaml
  production:
    values:
      - values/production.yaml

---
releases:
  ## Quiz

  - name: ktesias
    namespace: odysseia
    chart: ./charts/ktesias
    version: 0.0.1
    missingFileHandler: Error
    labels:
      tier: tests
    values:
      - values/{{ .Environment.Name }}.yaml
      - images:
          job:
            repo: ktesias
            tag: v0.0.2