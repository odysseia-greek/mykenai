environments:
  local:
    values:
      - values/local.yaml
  staging:
    values:
      - values/staging.yaml
  production:
    values:
      - values/production.yaml

---
releases:
  ## Quiz


  - name: meletos
    namespace: odysseia
    chart: ./charts/meletos
    version: 0.1.0
    missingFileHandler: Error
    labels:
      tier: quiz
    values:
      - values/{{ .Environment.Name }}.yaml
      - images:
          job:
            repo: meletos
            tag: v0.1.0
