apiVersion: v1
kind: Secret
metadata:
  name:  "{{ template "rabbit.uname" . }}"
  namespace: {{ template "rabbit.namespace" . }}
type: Opaque
data:
  default_user: {{ .Values.username | b64enc }}
  default_password: {{ .Values.password | b64enc }}
