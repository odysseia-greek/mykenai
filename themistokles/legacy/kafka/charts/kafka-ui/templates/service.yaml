apiVersion: v1
kind: Service
metadata:
  name: "{{ template "kafka-ui.name" . }}"
  namespace: {{ template "namespace" . }}
spec:
  selector:
    app: "{{ template "kafka-ui.name" . }}"
  ports:
    - name: http
      port: {{ .Values.envVariables.port }}
      protocol: TCP
