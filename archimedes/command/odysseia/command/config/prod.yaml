elastic:
  config:
    kibana: true
  volumeClaimTemplate:
    accessModes: [ "ReadWriteOnce" ]

apis:
  config:
    fullJobRun: true
    externalRepo: false

vault:
  server:
    ha:
      enabled: true

infra:
  config:
    environment: placeholder
    externalRepo: false
