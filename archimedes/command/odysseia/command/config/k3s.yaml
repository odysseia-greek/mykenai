elastic:
  volumeClaimTemplate:
    storageClassName: "local-path"

ingress:
  config:
    apis: true
    host: k3s-odysseia.greek
    apiHost: k3s-odysseia.api.greek
    ingress:
      class: traefik