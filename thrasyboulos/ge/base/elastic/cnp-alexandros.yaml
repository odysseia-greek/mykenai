apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-all-elasticsearch-access-alexandros
spec:
  endpointSelector:
    matchLabels:
      elasticsearch.k8s.elastic.co/cluster-name: aristoteles
  ingress:
    - fromEndpoints:
        - matchLabels:
            app: alexandros
            io.kubernetes.pod.namespace: makedonia
      toPorts:
        - ports:
            - port: "9200"
              protocol: TCP