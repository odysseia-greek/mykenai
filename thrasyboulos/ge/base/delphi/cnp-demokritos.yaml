apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-vault-access-demokrtios
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: vault
  ingress:
    - fromEndpoints:
        - matchLabels:
            app: demokrtios
            io.kubernetes.pod.namespace: makedonia
      toPorts:
        - ports:
            - port: "8200"
              protocol: TCP