apiVersion: builtin
kind: PatchTransformer
metadata:
  name: vault-volume-transformer
patch: |
  - op: add
    path: /spec/template/spec/volumes/-
    value:
      name: vault-server-tls
      secret:
        secretName: vault-server-tls
target:
  kind: Deployment
  annotationSelector: "volumes.odysseia.io/vault=true"