apiVersion: builtin
kind: PatchTransformer
metadata:
  name: add-common-env-to-cronjobs
patch: |
  - op: add
    path: /spec/jobTemplate/spec/template/spec/containers/0/envFrom/-
    value:
      configMapRef:
        name: odysseia-common-env
target:
  kind: CronJob
  name: ".*"
