apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/olympia
  - dionysios
  - aristarchos

labels:
  - pairs:
      environment: "local"
      variant: "k0s"
      domain: "backend"
    includeSelectors: false
    includeTemplates: true