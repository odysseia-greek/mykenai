apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homeros-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`k3d-odysseia.greek`) && PathPrefix(`/sokrates/graphql`)
      kind: Rule
      services:
        - name: sokrates
          port: 8080
