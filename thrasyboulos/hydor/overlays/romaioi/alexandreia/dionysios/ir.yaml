apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dioynsios-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`byzantium.odysseia-greek`) && PathPrefix(`/dioynsios/v1`)
      kind: Rule
      services:
        - name: dioynsios
          port: 5000
