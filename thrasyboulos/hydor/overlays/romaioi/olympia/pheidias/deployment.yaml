apiVersion: apps/v1
kind: Deployment
metadata:
  name: pheidias
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "pheidias"
  template:
    metadata:
      labels:
        app: "pheidias"
        release: pheidias
    spec:
      containers:
        - name: "pheidias"
          image: ghcr.io/odysseia-greek/pheidias
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              value: production
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 32Mi
            limits:
              memory: 32Mi
