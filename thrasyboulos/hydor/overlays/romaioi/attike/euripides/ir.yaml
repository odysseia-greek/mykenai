apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: euripides-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`attike.odysseia-greek`) && PathPrefix(`/euripides/graphql`)
      kind: Rule
      services:
        - name: euripides
          port: 8080
