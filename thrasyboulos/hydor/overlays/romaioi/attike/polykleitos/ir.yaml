apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: polykleitos-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`attike.byzantium.odysseia-greek`)
      kind: Rule
      services:
        - name: polykleitos
          port: 3000
