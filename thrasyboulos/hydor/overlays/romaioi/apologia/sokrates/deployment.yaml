apiVersion: apps/v1
kind: Deployment
metadata:
  name: sokrates
  labels:
    app: sokrates
  annotations:
    mount.odysseia.io/combined: "true"
    volumes.odysseia.io/combined: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sokrates
  template:
    metadata:
      labels:
        app: sokrates
        release: sokrates
        version: v0.2.2
      annotations:
        perikles/accesses: "aristippos;xenofon;kriton;kritias;alkibiades;antisthenes;aspasia"
        perikles/hostname: "sokrates"
        perikles/validity: "10"
    spec:
      containers:
        - name: sokrates
          image: ghcr.io/odysseia-greek/sokrates:v0.2.2
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: sokrates
          ports:
            - name: graphql
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
