apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: herodotos-graphql-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`hellas.odysseia-greek`) && PathPrefix(`/herodotos/graphql`)
      kind: Rule
      services:
        - name: herodotos
          port: 8080
